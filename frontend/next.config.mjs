/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Ajoute ici d'autres options si besoin, par exemple :
  // output: 'standalone',
  // images: { domains: ['localhost'] },
};

export default nextConfig;
;